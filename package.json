{
    "name": "luxury-travel-platform",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "dev:website": "cd website && npm run dev -- -p 3000",
        "dev:admin": "cd admin && npm run dev -- -p 3001",
        "build:website": "cd website && npm run build",
        "build:admin": "cd admin && npm run build",
        "install:all": "npm install --prefix website && npm install --prefix admin"
    }
}